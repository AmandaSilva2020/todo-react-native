import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 20,
        borderTopWidth: 1,
        borderColor: "#333333"
    },
    text: {
        fontWeight: "bold",
    }
})